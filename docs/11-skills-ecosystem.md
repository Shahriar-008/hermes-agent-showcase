# 71 Skills Across 18 Categories

> **Hermes learns from experience — every solved problem becomes a reusable skill. 71 skills installed, covering everything from infrastructure management to creative design, from ML model serving to social media automation.**

---

## Skills by Category

### Autonomous AI Agents (2)
| Skill | Description |
|---|---|
| `hermes-agent` | Configure, extend, or contribute to Hermes Agent itself |
| `autonomous-coding-agents` | Delegate coding to external AI CLI agents — Claude Code, Codex, OpenCode CLI |

### Creative (12)
| Skill | Description |
|---|---|
| `ascii-video` | Convert video/audio to colored ASCII MP4/GIF |
| `claude-design` | Design one-off HTML artifacts (landing, deck, prototype) |
| `comfyui` | Generate images, video, and audio with ComfyUI |
| `excalidraw` | Hand-drawn Excalidraw JSON diagrams (arch, flow, seq) |
| `humanizer` | Humanize text: strip AI-isms and add real voice |
| `image-forensics-fallbacks` | Inspect user-provided images when native vision is unavailable |
| `manim-video` | Manim CE animations: 3Blue1Brown math/algo videos |
| `p5js` | p5.js sketches: gen art, shaders, interactive, 3D |
| `pixel-art` | Pixel art with era palettes (NES, Game Boy, PICO-8) |
| `popular-web-designs` | 54 real design systems (Stripe, Linear, Vercel) as HTML/CSS |
| `pretext` | Browser demos with @chenglou/pretext — DOM-free text layout |
| `touchdesigner-mcp` | Control a running TouchDesigner instance via twozero MCP |

### Data Science (1)
| Skill | Description |
|---|---|
| `jupyter-live-kernel` | Iterative Python via live Jupyter kernel (hamelnb) |

### DevOps (4)
| Skill | Description |
|---|---|
| `cutie-dm-forwarder` | Cron-based DM transcript forwarder — monitors group-chat for Cutie's DMs |
| `delegation-discipline` | Enforce delegate-first patterns for Hermes orchestrator profiles |
| `web-scraping-monitors` | Build automated web scrapers and change-detection monitors |
| `webhook-subscriptions` | Webhook subscriptions: event-driven agent runs |

### Development Methodology (1)
| Skill | Description |
|---|---|
| `development-methodology` | Complete pipeline — plan, spike, implement, TDD, verify |

### Debugging (1)
| Skill | Description |
|---|---|
| `debugging-workflows` | 4-phase root cause methodology, Python/Node.js debuggers |

### Email (1)
| Skill | Description |
|---|---|
| `himalaya` | Himalaya CLI: IMAP/SMTP email from terminal |

### Gaming (2)
| Skill | Description |
|---|---|
| `minecraft-modpack-server` | Host modded Minecraft servers (CurseForge, Modrinth) |
| `pokemon-player` | Play Pokemon via headless emulator + RAM reads |

### MCP (1)
| Skill | Description |
|---|---|
| `native-mcp` | MCP client: connect servers, register tools (stdio/HTTP) |

### Media (3)
| Skill | Description |
|---|---|
| `ai-music` | AI-assisted music creation — songwriting craft, Suno AI prompts |
| `spotify` | Spotify: play, search, queue, manage playlists and devices |
| `youtube-content` | YouTube transcripts to summaries, threads, blogs |

### Messaging (2)
| Skill | Description |
|---|---|
| `daily-user-engagement` | Recurring cron-based personalized engagement messages |
| `telegram-gateway-setup` | Reliably install and configure Hermes Gateway for Telegram |

### MLOps (9)
| Skill | Description |
|---|---|
| `dspy` | DSPy: declarative LM programs, auto-optimize prompts, RAG |
| `evaluating-llms-harness` | lm-eval-harness: benchmark LLMs (MMLU, GSM8K, etc.) |
| `huggingface-hub` | HuggingFace hf CLI: search/download/upload models, datasets |
| `llama-cpp` | llama.cpp local GGUF inference + HF Hub model discovery |
| `llm-router-configuration` | Configure LLM model routers (Manifest, etc.) for cost-optimized inference |
| `obliteratus` | OBLITERATUS: abliterate LLM refusals (diff-in-means) |
| `segment-anything-model` | SAM: zero-shot image segmentation via points, boxes, masks |
| `serving-llms-vllm` | vLLM: high-throughput LLM serving, OpenAI API, quantization |
| `weights-and-biases` | W&B: log ML experiments, sweeps, model registry, dashboards |

### Note-Taking (1)
| Skill | Description |
|---|---|
| `obsidian` | Read, search, create, and edit notes in the Obsidian vault |

### Productivity (9)
| Skill | Description |
|---|---|
| `airtable` | Airtable REST API via curl — records CRUD, filters, upserts |
| `google-workspace` | Gmail, Calendar, Drive, Docs, Sheets via gws CLI or Python |
| `habit-tracker` | Manage daily habits via the habitrack CLI |
| `linear` | Linear: manage issues, projects, teams via GraphQL + curl |
| `maps` | Geocode, POIs, routes, timezones via OpenStreetMap/OSRM |
| `notion` | Notion API + ntn CLI: pages, databases, markdown, Workers |
| `ocr-and-documents` | Extract text from PDFs/scans and edit PDF text |
| `polytrend-automation` | Maintain the PolyMarket Trend automation — 13 scheduled jobs |
| `powerpoint` | Create, read, edit .pptx decks, slides, notes, templates |
| `teams-meeting-pipeline` | Operate the Teams meeting summary pipeline |

### Red Teaming (1)
| Skill | Description |
|---|---|
| `godmode` | Jailbreak LLMs: Parseltongue, GODMODE, ULTRAPLINIAN |

### Research (5)
| Skill | Description |
|---|---|
| `arxiv` | Search arXiv papers by keyword, author, category, or ID |
| `domain-intel` | Passive domain reconnaissance using Python stdlib |
| `llm-wiki` | Karpathy's LLM Wiki: build/query interlinked markdown KB |
| `polymarket` | Query Polymarket: markets, prices, orderbooks, history |
| `research-paper-writing` | Write ML papers for NeurIPS/ICML/ICLR: design→submit |

### Smart Home (1)
| Skill | Description |
|---|---|
| `openhue` | Control Philips Hue lights, scenes, rooms via OpenHue CLI |

### Social Media (1)
| Skill | Description |
|---|---|
| `xurl` | X/Twitter via xurl CLI: post, search, DM, media, v2 API |

### Software Development (4)
| Skill | Description |
|---|---|
| `hermes-s6-container-supervision` | Modify, debug, or extend the s6-overlay supervision tree |
| `memory-hygiene` | Protocols for keeping Mnemosyne memory clean |
| `python-cli-packaging` | Package a Python CLI script into a modern pip-installable package |
| `simplify-code` | Parallel 3-agent cleanup of recent code changes |

### Writing (1)
| Skill | Description |
|---|---|
| `shahriar-voice` | Shahriar's complete writer profile — blog, social media, and web copy voice |

---

## Production-Critical Skills

These skills are actively used in automated workflows:

| Skill | Used By |
|---|---|
| `polytrend-automation` | 13 market-channel cron jobs |
| `delegation-discipline` | Watchdog + self-audit cron jobs |
| `daily-user-engagement` | 4 personal check-in cron jobs |
| `cutie-dm-forwarder` | 1 DM forwarding cron job |
| `github-workflows` | Daily autobackup cron job |
| `habit-tracker` | Todo check-in cron jobs |
| `telegram-gateway-setup` | All 3 gateway services |
| `llm-router-configuration` | Every LLM call (Manifest Router) |
| `shahriar-voice` | All user-facing content generation |
| `server-infrastructure-management` | Biweekly gateway restart, backup verification |

---

## Skill Lifecycle

Skills are Hermes's procedural memory. The curator system manages their lifecycle:

| Stage | What Happens |
|---|---|
| **Active** | Skill is used regularly, kept up to date |
| **Idle** | Not used recently, marked for review |
| **Stale** | No usage for extended period, candidate for archival |
| **Archived** | Moved out of active rotation (never deleted — always recoverable) |
| **Pinned** | Protected from any automatic lifecycle transitions |

Skills are created when a complex task succeeds (5+ tool calls), when errors are overcome, or when a user-corrected approach works. They are patched whenever they become stale or wrong.

---

## The Philosophy

> *Every bug fixed twice is a bug fixed once. Every problem solved once becomes a skill that solves it forever.*

Skills turn hard-won debugging sessions into reusable knowledge. The next time the same class of problem appears, the skill provides the exact commands, pitfalls, and verification steps — no rediscovery needed.
